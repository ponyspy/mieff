extends ../layout

block header_meta
	- var content_title= 'ЗАЯВКИ'

block header_include
	- var include_css = ['add.css']
	- var include_js = ['lists.js']

block sub_menu
	a.sub_item.back_link(href='/admin') Назад
	span.sub_item.toggle_rm Удалить
	.sub_right_block

		.sub_drop.type(style='z-index: 1;')
			.drop_title Тип
			.drop_inner
				.drop_item(class='all').select Все
				.drop_item(class='opencall') Open call

		//- input.sub_search(type='text', placeholder='Поиск...')

block content
	.title= content_title
	br
	//- a.add(href='/admin/applications/add') + Добавить заявку
	br
	br
	.lists_block
		include _applications